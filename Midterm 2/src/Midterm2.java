public class Midterm2 {

/*
if(begin.getNext()==null){
    Node<T> a = new Node( element,null);
    begin.setNext(a);

}else if(begin.getNext()!=null) {
Node <T> next = begin.getNext();
Node<T> a = new Node(element, next);
begin.setNext(a);
}

public class GenList <T> {
      private NGen <T> begin;

      // default constructor creates empty, headed list
      public GenList () {
          begin = new NGen<T> (null, null);
      }

      public void addToStart(T item) {
          if (begin==null){

Node<T> a = new Node( element,null);
begin = a;

 }else if(head!=null) {
Node a = new Node(element, begin);
}
//sorry for weird indenting, copying and pasting from intelij messed it up
int currCount = 0;
if (begin == null) { //empty list
    return currCount;
} else if (begin != null) { //traverses list and increases size each node
    Node<T> currNode = begin;

    while (currNode.getNext() != null) {
if(currNode.getData() == null){
   currCount++;
}
        currNode = currNode.getNext();
    }
}
return size;
 */

}
